# ============================================================
#  SMTP Mail Server - Environment Configuration
#  Copy this file to .env and customize the values below
# ============================================================

# ----------------------------------------------------------
# DOMAIN SETTINGS
# ----------------------------------------------------------

# Your primary mail domain (the part after @ in email addresses)
MAIL_DOMAIN=example.com

# Mail server hostname (must have a DNS A record pointing to your server IP)
MAIL_HOSTNAME=mail.example.com

# Additional domains to handle mail for (comma-separated, optional)
# Leave empty if you only use one domain
ADDITIONAL_DOMAINS=notification.example.com

# ----------------------------------------------------------
# SSL / TLS CERTIFICATE
# ----------------------------------------------------------

# SSL mode: letsencrypt, selfsigned, or manual
#   letsencrypt  = Auto-obtain free certificate (port 80 must be open)
#   selfsigned   = Generate self-signed certificate (shows warnings in clients)
#   manual       = Use your own certificates (mount them to the container)
SSL_MODE=letsencrypt

# Email for Let's Encrypt notifications (required if SSL_MODE=letsencrypt)
SSL_EMAIL=admin@example.com

# ----------------------------------------------------------
# MAIL ACCOUNTS
# ----------------------------------------------------------

# Email accounts to create (format: user@domain:password)
# Separate multiple accounts with | (pipe character)
# Passwords will be hashed automatically with SHA512-CRYPT
#
# Example: admin@example.com:MyStr0ngP@ss|info@example.com:An0therP@ss
MAIL_ACCOUNTS=admin@example.com:changeme123|info@example.com:changeme456

# ----------------------------------------------------------
# MAIL ALIASES (FORWARDING)
# ----------------------------------------------------------

# Email aliases/forwarding rules (format: alias@domain:target@domain)
# Separate multiple aliases with | (pipe character)
#
# Example: contact@example.com:admin@example.com|support@example.com:admin@example.com
MAIL_ALIASES=contact@example.com:admin@example.com|support@example.com:admin@example.com

# ----------------------------------------------------------
# ADVANCED SETTINGS
# ----------------------------------------------------------

# Maximum email message size in bytes (default: 50MB = 52428800)
MESSAGE_SIZE_LIMIT=52428800

# Timezone (default: UTC)
TZ=UTC
